server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/plant_cultivation?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: plant_cultivation
    password: ${DB_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: ${JWT_SECRET:YXB0LXBsYW50LWN1bHRpdmF0aW9uLXNlY3JldC1rZXktMjAyNg==}
  expiration: 86400000  # 24 hours

app:
  upload-dir: ${UPLOAD_DIR:/www/wwwroot/uploads}

logging:
  level:
    com.plantcultivation: debug
    com.zaxxer.hikari: DEBUG
    org.springframework.jdbc: DEBUG
